// Generated by Qleany v{{ s.system.inner.version }} from {{ s.file.inner.template_name }}.tera

//! Layer A -- reactive collections over a Qleany relationship.
//!
//! One model per relationship field marked `list_model: true`. Each reads the
//! *owner's* relationship rather than `get_all_*`: the store is shared, so
//! `get_all` would merge every other owner's children into this list, and it
//! returns database-key order rather than the order the user chose.
//!
//! Like the singles, each file carries a real and a mock arm behind two
//! `#[cfg]`-gated `mod imp` blocks with one public surface.

pub mod coalesced_reload;

{%- for eid, ent in s.entities %}
{%- if not ent.inner.only_for_heritage %}
{%- for f in ent.fields %}
{%- if f.inner.list_model and f.inner.entity %}
mod {{ ent.snake_name }}_{{ f.snake_name }}_list_model;
{%- endif %}
{%- endfor %}
{%- endif %}
{%- endfor %}

{%- for eid, ent in s.entities %}
{%- if not ent.inner.only_for_heritage %}
{%- for f in ent.fields %}
{%- if f.inner.list_model and f.inner.entity %}
pub use {{ ent.snake_name }}_{{ f.snake_name }}_list_model::{
    {{ ent.pascal_name }}{{ f.pascal_name }}ListModel, {{ ent.pascal_name }}{{ f.pascal_name }}Row,
};
{%- endif %}
{%- endfor %}
{%- endif %}
{%- endfor %}
